export class Engine { 
 
    constructor(canvas: HTMLCanvasElement) {
        // hook onto canvas in HTML
    }

    /**
     * Updates the simulation by a given tick amount. 
     * @param dt number of ticks to advance
     */
    update(dt: number) : void {

    }
    
    /**
     * Handles rendering
     */
    render() : void {

    }

    
}